#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int f,m,b,tf,tm,tb;
    cin>>f>>m>>b>>tf>>tm>>tb;
    cout<<(tf + tm + tb)<<' '<<(tf*f + tm*m + tb*b)<<'\n';
}
